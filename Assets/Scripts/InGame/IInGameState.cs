using UnityEngine;

/// <summary>
/// インゲームの各ステートのインターフェース
/// </summary>
public class IInGameState : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
